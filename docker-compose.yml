# ==============================================================
# docker-compose.yml — Smart AI Lighting Catalog
# ==============================================================

version: "3.9"

services:
  smart_lighting_api:
    build: .
    container_name: smart_lighting_api
    ports:
      - "8000:8000"
    env_file:
      - .env.example
    volumes:
      - .:/app
    restart: always
